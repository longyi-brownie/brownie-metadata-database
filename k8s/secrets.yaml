apiVersion: v1
kind: Secret
metadata:
  name: brownie-metadata-secrets
  namespace: brownie-metadata
type: Opaque
data:
  # Database client certificate (no passwords - certificates only)
  # WARNING: These should be generated from Vault or local files, not hardcoded!
  # Use: kubectl create secret generic brownie-metadata-secrets --from-file=client.crt --from-file=client.key --from-file=ca.crt
  database-client-cert: "PLACEHOLDER_DO_NOT_USE"
  database-client-key: "PLACEHOLDER_DO_NOT_USE" 
  database-ca-cert: "PLACEHOLDER_DO_NOT_USE"
